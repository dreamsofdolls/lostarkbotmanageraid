# Changelog

All notable changes to this project will be documented in this file. Dates use the local calendar of the commit.

## 2026-04-21

### Fixed

- **`/raid-channel repin` now deletes the old welcome instead of just unpinning it.** Real-user test showed: running repin produced a fresh welcome + pinned it, but the old welcome stayed visible in the channel as a regular (unpinned) message — exactly the clutter that repin was supposed to clear. `postRaidChannelWelcome()` now calls `oldMsg.delete()` on the previously-stored welcome after the new one is post+pin+persist confirmed (delete auto-removes from pin list, so no separate unpin call needed). Safe-order preserved: partial failure on the fresh-welcome path still leaves the old welcome pinned + visible as a fallback so the channel never loses guidance. Outcome field renamed `unpinnedCount` → `removedOldCount` to match reality; repin's reply embed label updated accordingly. `src/raid-command.js` (postRaidChannelWelcome, repin reply).

### Fixed (round 17 Codex review)

- **[LOW] `postRaidChannelWelcome()` rolls back the fresh pin on `welcomeMessageId` persist failure.** Round 16 added `outcome.persisted` so the unpin-old step would skip when the Mongo write failed, keeping the previous welcome pinned as a fallback. But the fresh welcome itself was still pinned in the channel — and because the DB never got the new ID, subsequent `/raid-channel repin` would unpin the OLD welcome (the one still tracked in DB) and leave the orphaned fresh pin behind as a stale ghost. The persist-failure path now calls `sent.unpin()` as a best-effort rollback so the channel state matches what the DB knows, and `outcome.pinned` flips to false so the `set` reply surfaces the failure to admin honestly. `src/raid-command.js` (postRaidChannelWelcome persist catch branch). (Codex round 17, Low.)
- **[LOW] README cooldown-hit row documents the one-exception-per-window limit.** HELP_SECTIONS got updated in round 16 when the `lastExceptionAt` per-user tracker landed, but the README's error-UX table row still described the pending-hint exception as an unqualified pass-through. Admin reading docs would miss that multiple different-content retries within the same window are dropped. Added "1 lần duy nhất mỗi cooldown window" to the row. `README.md`. (Codex round 17, Low.)

### Fixed (round 16 Codex review)

- **[MEDIUM] Pending-hint exception is now limited to one retry per cooldown window.** Round 15 added the `hasPendingHint && !sameContent` exception so a user could correct a typo within 2 seconds, but since every failed attempt through the error path replaces the pending hint with a fresh one, the "pending hint exists" state kept resetting — a user could spam unlimited different-content attempts (`Fake1`, `Fake2`, `Fake3`…) and all would pass through. Moved exception tracking from per-hint to per-user via `lastExceptionAt` on the cooldown entry: the exception slot is consumed on first grant and refills only when the next cooldown window resets. `checkUserMonitorCooldown` returns `viaException` so `commitUserMonitorActivity(message, viaException)` can bump `lastExceptionAt = now` on exception accepts and reset to 0 on clean cooldown-expired accepts. Legit typo→fix still works (one correction per typo-within-cooldown), but chained mutation spam is now throttled into the normal spam-count + warning path. `src/raid-command.js` (cooldown entry adds `lastExceptionAt`; `checkUserMonitorCooldown` returns `viaException`; handler threads it into `commitUserMonitorActivity`). (Codex round 16, Medium.)
- **[LOW] `postRaidChannelWelcome()` skips unpinning the old welcome when `welcomeMessageId` persist fails.** Round 14 fixed persist-on-pin-success (not on post-only), but the unpin-old-welcome step still ran on just `posted && pinned`, so a Mongo write failure would leave the DB pointing at the stale old ID while the old welcome got unpinned anyway — next repin lost track of which welcome was actually pinned. Outcome now carries a `persisted` flag; unpin-old requires `posted && pinned && persisted`. Partial failure = keep old pinned, admin can re-run repin later and the persisted DB ID matches reality. When no `guildId` is passed (standalone helper use), `persisted` is treated as true so unpin-old still runs. `src/raid-command.js` (postRaidChannelWelcome outcome + guard). (Codex round 16, Low.)
- **[LOW] `/raid-help` notes synced with the last 4 rounds of behavior changes.** The HELP_SECTIONS text for `/raid-channel` still described cooldown as simple "parse-success 2s" and set/repin as "unpin old then post", even though rounds 13–15 made the cooldown content-aware, turned set/repin into safe-order, added one-retry-per-cooldown exception limits, and cascaded `autoCleanupEnabled` from clear. Rewrote the per-subcommand bullets and the cooldown bullet to reflect current reality. `src/raid-command.js` (HELP_SECTIONS raid-channel notes). (Codex round 16, Low.)

### Fixed (round 15 Codex review)

- **[MEDIUM] Cooldown is now content-aware and drops duplicate spam in recoverable-error paths.** Round 14 moved `commitUserMonitorActivity` to the success-only branch to preserve the typo-then-correct flow, but that also meant recoverable-error spam (e.g. `Serca Nightmare Nothinghere` × 20) bypassed the cooldown entirely — each duplicate still triggered a full applyRaidSetForDiscordId read + postPersistentHint. Cooldown now tracks `lastContent` per user and drops duplicate content within the 2-second window, while letting **different content during an active hint flow** pass through (preserving round 14's correction UX). Committed unconditionally right after check-pass since the content-aware rule handles the round-14 case without deferring. Content-identical spam is throttled to ~1 attempt per 2 s regardless of whether it's a success or error path. `src/raid-command.js` (checkUserMonitorCooldown rewrite + lastContent field, handler commit moved to top of accepted branch). (Codex round 15, Medium.)
- **[LOW] README cooldown-hit row describes actual behavior.** Table row said "Silent drop", but round-13 code already deletes the throttled message. Updated to describe deletion + warning thresholds + the pending-hint exception. `README.md`. (Codex round 15, Low.)
- **[LOW] Schedule-enable comment now matches code behavior.** Old comment claimed "disabling doesn't touch the key so re-enabling later keeps missed-day catch-up behavior intact", but the enable path always stamps today's VN day key — so re-enable after days off is a fresh start, not a catch-up. Rewrote the comment to describe the actual "enable = start fresh, bot-offline catch-up works only during continuous-enable" semantics. No behavior change; the fresh-start semantics are intentional (turning schedule on shouldn't immediately purge backlog). `src/raid-command.js` (handleRaidChannelCommand schedule comment). (Codex round 15, Low.)

### Fixed (round 14 Codex review)

- **[MEDIUM] Spam cooldown timestamp is now bumped only on successful writes.** Previous implementation recorded `lastProcessedAt` on every accepted message — so a user who typoed, saw a hint, and corrected within <2 seconds had their correct retry dropped + deleted as if it were spam. Split `recordUserMonitorActivity` into `checkUserMonitorCooldown(message)` (peek + spam tracking, no timestamp bump) and `commitUserMonitorActivity(message)` (bump timestamp + reset spam counters). Handler calls the check at the top of the gate and the commit only at the end of the true-success path. Error paths (wrong char, iLvl too low, mode mismatch returned from `applyRaidSetForDiscordId`) skip the commit so users can iterate on corrections fast. True spam still throttled because the first successful write DOES commit. `src/raid-command.js` (userMonitorCooldowns helpers, handleRaidChannelMessage success path). (Codex round 14, Medium.)
- **[MEDIUM] `/raid-channel clear` now cascades `autoCleanupEnabled=false`.** Previous implementation cleared only `raidChannelId`, leaving the schedule flag intact. If admin later `/set`s a fresh channel, the scheduler would re-fire auto-cleanup on it without admin explicitly opting back in — potentially purging the new channel's content unexpectedly. `clear` now atomically clears both fields in the same `findOneAndUpdate`, and the success embed explicitly surfaces that the schedule was reset so admin knows they need to re-run `schedule action:on` if they want auto-cleanup back. `src/raid-command.js` (handleRaidChannelCommand clear branch). (Codex round 14, Medium.)
- **[LOW] `postRaidChannelWelcome()` persists `welcomeMessageId` only after both post AND pin succeed.** Round-13 moved the unpin-old step to after post+pin, but the ID persistence still happened on post-success alone. If `sent.pin()` failed (network blip, rate limit), the DB would point at an unpinned new welcome while the actually-pinned old welcome stayed in place — the next repin couldn't find the pinned one to remove and would keep piling up welcomes. Persistence now lives inside the pin-success branch. `src/raid-command.js` (postRaidChannelWelcome). (Codex round 14, Low.)

### Fixed (round 13 Codex review)

- **[MEDIUM] `/raid-channel schedule action:on` now stamps today's VN day key on enable.** Previous implementation flipped `autoCleanupEnabled=true` without touching `lastAutoCleanupKey`, so if admin enabled the schedule mid-day (e.g. 15:00 VN), the next 30-minute scheduler tick would see `lastAutoCleanupKey !== today` and immediately run a "catch-up" cleanup instead of waiting for the next 00:00 VN boundary. The enable path now sets `lastAutoCleanupKey = getTargetCleanupDayKey()` atomically in the same `findOneAndUpdate`, so the first real auto-run fires after the next VN-midnight. Disabling leaves the key alone so re-enabling later still preserves missed-day catch-up semantics. `src/raid-command.js` (handleRaidChannelCommand schedule branch). (Codex round 13, Medium.)
- **[MEDIUM] Spam-guard now also deletes the throttled message.** Round previous added per-user cooldowns that silent-drop parse-success messages but left the source messages visible in the channel — a sustained spammer could dirty the channel with ignored raid-clear attempts even though Artist correctly skipped the DB/DM/API work. Handler now calls `message.delete()` (best-effort, swallow errors) on every cooldown-dropped message so the channel stays clean regardless of spam intent. Parse-null chat is still unaffected since it short-circuits before the cooldown check. `src/raid-command.js` (handleRaidChannelMessage cooldown-drop branch). (Codex round 13, Medium.)
- **[LOW] `postRaidChannelWelcome()` posts + pins the new welcome before unpinning the old one.** Earlier implementation tore down the old pin first; if the subsequent `channel.send` or `msg.pin` failed (network hiccup, rate limit, permission blip), the channel would end up with NO pinned welcome at all until admin manually re-ran `repin`. The helper now only calls `oldMsg.unpin()` after confirming the new message was both posted and pinned successfully — any failure in the new-welcome path leaves the old pinned welcome in place so the channel always has guidance visible. `src/raid-command.js` (postRaidChannelWelcome). (Codex round 13, Low.)

### Added

- **Per-user spam guard on the raid channel monitor.** Sustained spam of parse-success messages (e.g. 20 `Serca Nightmare <someone>` in 2 seconds) would previously fan out to fetch + delete + reply + DM + DB write per message, risking Discord rate limits and wasting cycles. Added `recordUserMonitorActivity(message)` gate: minimum **2 seconds** between processed messages per user, silent-drop on cooldown. A sliding 10-second window counts cooldown hits; ≥3 hits within the window triggers a single **kitsune-style warning** ("🦊💢 Này ơi, Artist theo không kịp đâu~ Mỗi tin cách nhau ít nhất 2 giây thôi nhé…") auto-deleting after 15s, deduped per-user for 60s so Artist doesn't pile-on. Parse-null messages (normal chat) remain unaffected since they already no-op at the silent-ignore branch. Welcome embed, help notes, and README error-UX table all document the 2-second rule. `src/raid-command.js` (new `userMonitorCooldowns`, `recordUserMonitorActivity`, `postSpamWarning`; `handleRaidChannelMessage` gate insertion). Motivation: Traine flagged that spam wasn't handled and Artist should "nổi quạo lên" instead of silently absorbing it.

### Fixed (round 12 Codex review)

- **[MEDIUM] `cleanupRaidChannelMessages()` now paginates instead of capping at 100.** Both `/raid-channel cleanup` and the daily auto-cleanup scheduler were fetching a single 100-message batch. Channels with more history would leave older orphan messages behind and miss-count `skippedOld`. The helper now iterates with Discord's `before` cursor, bulk-deleting non-pinned messages batch by batch, and stops when a short batch signals end-of-history or after 20 iterations (2000-message safety cap). `src/raid-command.js` (cleanupRaidChannelMessages). (Codex round 12, Medium.)
- **[MEDIUM] `/raid-channel repin` now unpins the exact stored welcome instead of every bot-authored pin.** Previous implementation walked `channel.messages.fetchPinned()` and unpinned any message authored by the bot — which would tear down unrelated bot pins sitting in the same channel. Added `welcomeMessageId: String` to `GuildConfig`; `postRaidChannelWelcome` reads the previous ID, fetches + unpins that specific message, then persists the new welcome's ID. `set` uses the same helper so both code paths stay coherent. `src/schema/guildConfig.js`, `src/raid-command.js` (postRaidChannelWelcome signature + persistence). (Codex round 12, Medium.)
- **[MEDIUM] `/raid-channel schedule action:on` rejects when no channel is configured.** The scheduler filters configs on `raidChannelId != null`, so enabling the schedule on a guild without a channel silently produced a success embed for a job that never runs. The schedule handler now checks the monitor channel cache before flipping `autoCleanupEnabled` to true and returns an error reply asking admin to run `set` first. Disabling with `action:off` remains unconditional. `src/raid-command.js` (handleRaidChannelCommand schedule branch). (Codex round 12, Medium.)
- **[LOW] README + HELP_SECTIONS list all five required channel permissions.** Permission gate was extended in round 10 to include `Read Message History` (for hint-cleanup `fetch`) and `Embed Links` (for welcome + DM embeds), but deploy docs still listed only the original three. Admins inviting with the old minimal set would hit `/raid-channel set` rejections. README prerequisites + HELP_SECTIONS `/raid-channel` notes now enumerate all five with short rationale per perm. `README.md`, `src/raid-command.js` (HELP_SECTIONS). (Codex round 12, Low.)

### Added

- **`/raid-channel` gets three new subcommands: `cleanup`, `repin`, `schedule`.**
  - `cleanup` — manual trigger that deletes every non-pinned message in the monitor channel via `channel.bulkDelete(messages, true)`. Pinned welcome survives. Messages older than 14 days are reported as "Skipped (>14 ngày)" because Discord rejects them from the bulk API. Ephemeral reply with count.
  - `repin` — unpins every bot-authored pinned message in the channel, then posts + pins a fresh welcome embed. Fixes the "welcome text is stale after a code change" case and the accumulated-pin case. Uses shared `postRaidChannelWelcome(channel, botUserId)` helper (the same path `/raid-channel set` now uses, so set and repin never drift).
  - `schedule action:on|off` — toggles per-guild daily auto-cleanup. When on, a 30-minute background tick checks each VN-midnight boundary; once per VN day per guild it runs the same cleanup logic above and stamps `lastAutoCleanupKey = "YYYY-MM-DD"` so ticks within the same VN day short-circuit. Bot offline across midnight catches up on first tick after restart.

  New `GuildConfig` fields `autoCleanupEnabled` (Boolean, default false) + `lastAutoCleanupKey` (String, default null). Scheduler lives in `startRaidChannelScheduler(client)` exported from `src/raid-command.js` and wired into `ClientReady` in `src/bot.js`. The scheduler runs always (not gated by `TEXT_MONITOR_ENABLED`) because auto-cleanup is a useful housekeeping action even when the text monitor itself is deploy-disabled.

### Fixed

- **Pending hint cleanup now also deletes the user's original failed message.** Previously the pending-hint entry stored only the bot's hint id, so on successful retry the hint vanished but the user's typo message stayed behind as orphan chat. `pendingChannelHints` entries now carry `originalId` alongside `hintId`, and `clearPendingHint` deletes both in `Promise.allSettled` best-effort — retry success, replacement hint, and 5-minute TTL all leave the channel clean. `src/raid-command.js` (pendingChannelHints entry shape, postPersistentHint, clearPendingHint). Motivation: real-user test showed the failed "Serca Nightmare Nothinghere" message + bot hint both sitting after a successful retry.

### Fixed (round 11 Codex review)

- **[LOW] Monitor cache is now loaded unconditionally at boot, regardless of `TEXT_MONITOR_ENABLED`.** Round 10 guarded the `loadMonitorChannelCache()` call behind the deploy flag. That left `/raid-channel show` blind to any `raidChannelId` already persisted in Mongo when the monitor was deploy-disabled — the cache was empty, so show reported "chưa config channel nào" even when stale config existed. Cache-load is pure DB I/O with zero Discord dependency, so loading it always is cheap and keeps `show` coherent with reality. `src/bot.js` (startBot cache-load condition removed).
- **[LOW] `/raid-channel show` now appends deploy notes in both config branches.** Previously the `deployNotes` list (monitor-disabled warning, cache-unhealthy warning) was only concatenated into the "no channel configured" branch of the description. When a channel WAS configured, admins would not see the deploy-level warnings at all — defeating the diagnostic purpose. Both branches now append deployNotes after the channel/permission status so flags stay visible regardless of config state. `src/raid-command.js` (handleRaidChannelCommand show branch).
- **[LOW] README DM-confirmation paragraph synced with the public fallback behavior.** The paragraph still said "DM fail → chỉ missing notification private", which contradicted the error-UX table above it (and the round-10 code). Updated to describe the public 15s transient fallback + the DM privacy toggle hint. `README.md`.

### Fixed (round 10 Codex review)

- **[MEDIUM] `/raid-channel set` refuses to save when `TEXT_MONITOR_ENABLED=false`.** Previously admin could set a channel + get a success embed + trigger the pinned welcome even when the bot process had `MessageContent` intent turned off at the deploy layer — members would then see the welcome, post raid-clears, and watch the bot silently ignore them. `set` now short-circuits with a warning when the deploy flag is off and leaves config unchanged. `show` surfaces the same flag state as a deploy note alongside channel info so admins can spot mismatches without digging through logs. `src/raid-command.js` (new `isTextMonitorEnabled`, `/raid-channel set` + `show` branches).
- **[MEDIUM] `loadMonitorChannelCache()` failure is now visible.** Previously Mongo failures during boot cache-load were logged and swallowed; the monitor would then silently ignore every message until restart because the cache looked "empty" instead of "failed". Added `monitorCacheHealthy` + `monitorCacheLoadError` module state set by `loadMonitorChannelCache`, elevated the log from `console.warn` to `console.error`, and exposed `getMonitorCacheHealth()` to `/raid-channel show` so admins see "cache chưa load được" with the underlying error instead of "chưa config channel". `src/raid-command.js` (loadMonitorChannelCache, new health getter, `/raid-channel show` branch).
- **[MEDIUM] Permission check adds `Read Message History` + `Embed Links`.** The previous `BOT_CHANNEL_PERMS` list only covered `View Channel` / `Send Messages` / `Manage Messages`. But `clearPendingHint` uses `channel.messages.fetch(id)` which silently fails without Read Message History (hints pile up instead of auto-cleaning on retry), and the welcome + success embeds require Embed Links or Discord strips them entirely. Both perms are now part of the `/raid-channel set` gate and the `/raid-channel show` health check so a broken install surfaces immediately. `src/raid-command.js` (BOT_CHANNEL_PERMS).
- **[LOW] DM confirmation failure now falls back to a public transient ping.** The previous success path tried `message.author.send(...)` and silently logged on failure — users with DMs disabled would see their raid-clear message disappear with zero feedback at all. `handleRaidChannelMessage` now separates the DM attempt from the cleanup batch; on DM failure, it posts a short channel message mentioning the user + raid + char, auto-deleting after 15 seconds. Message mentions the "Allow DMs from server members" privacy toggle so users know how to restore private confirmations. `src/raid-command.js` (success path restructured).
- **[LOW] Welcome embed text synced with persistent-hint behavior.** Round 9 switched recoverable-error replies from transient 10s auto-delete to persistent ping-with-retry-cleanup + 5-min TTL, but the pinned welcome embed still advertised "tự dọn sau 10 giây". Updated the relevant bullet + added a bullet describing DM success vs public fallback. `src/raid-command.js` (buildRaidChannelWelcomeEmbed).

### Added

- **Raid channel success now DMs the user a private confirmation embed.** Discord only supports ephemeral (author-only-visible) messages as part of interaction replies — not on `MessageCreate` events — so the closest equivalent for the text monitor success path is a direct message. After a successful `applyRaidSetForDiscordId`, the bot now builds a `buildRaidChannelSuccessEmbed` (Character / Raid / Gates fields, mode-switch footer when applicable) and sends it via `message.author.send(...)` in parallel with the original-message delete and hint cleanup. DM failures (user disabled DMs from server members) are caught and logged with the user tag — data is still saved, the user just misses the notification. Keeps the public channel clean while giving each user a readable trail of their own clears in DMs. `src/raid-command.js` (new `buildRaidChannelSuccessEmbed`, success path in `handleRaidChannelMessage`).

### Changed

- **Recoverable errors in the raid channel now use persistent ping-replies that auto-clean on retry.** Previously every error (multi-gate, character-not-found, iLvl-too-low, invalid raid combo, invalid gate, no roster) went through `postTransientReply` — a reply with `allowedMentions.repliedUser: false` that auto-deleted after 10 seconds. User often missed the feedback entirely if they weren't looking at the channel, and even when they saw it, there was no retry-aware cleanup. New `postPersistentHint` path replies **with default reply-mention** (pings the user via Discord's reply ping), tracks the hint in a per-user `pendingChannelHints` Map keyed by `guildId:channelId:userId`, and auto-clears the hint when the same user next posts a recoverable-error or a success message in that channel. TTL fallback of 5 minutes cleans up hints if the user never retries. Transient replies are kept only for truly non-actionable internal errors (DB/Discord failures the user can't fix by retyping). `src/raid-command.js` (new `pendingChannelHints`, `hintKey`, `clearPendingHint`, `postPersistentHint`; `handleRaidChannelMessage` dispatch rewired). Motivation: first real-user test hit a typo case where the original transient reply and silent-ignore both felt opaque — user needed a real notification and a cleaner retry flow.

### Added

- **`/raid-channel set` now posts a pinned welcome embed in the target channel.** Previously only the invoking admin saw the success ephemeral; guild members who stumbled into the channel had no onboarding and would have to run `/raid-help` separately. After the permission check passes and the config is saved, the bot now sends a public embed (`buildRaidChannelWelcomeEmbed`) into the channel documenting the message format, every raid/difficulty/gate alias, and the "self-roster only" rule — then auto-pins it so the guidance stays at the top. The embed uses a warm, lightly-personified Vietnamese voice and refers to the bot as **Artist** (the public-facing persona) so members have a consistent name to reference. Pin and send failures are surfaced back to the admin in the ephemeral reply (`posted` / `posted (pin failed)` / `NOT posted`). When admin re-runs `set` for a different channel, the admin reply reminds them to unpin/delete the old welcome message manually. `src/raid-command.js` (new `buildRaidChannelWelcomeEmbed`, `handleRaidChannelCommand` set branch).

### Fixed (round 9 Codex review)

- **[MEDIUM] Monitor channel cache now loads BEFORE `client.login()` instead of inside `ClientReady`.** Round 8 placed `loadMonitorChannelCache()` in the `ClientReady` handler, which runs after the `MessageCreate` listener is already registered. In the async window between `client.login()` resolving and `ClientReady` finishing, incoming messages would hit an empty cache and get dropped as "no monitor configured." Cache loading is pure DB I/O with no Discord dependency, so moving the call above `client.login()` removes the race window entirely. `src/bot.js` (startBot sequence reorder). (Codex round 9, Medium.)
- **[LOW] `/raid-channel clear` always write-through Mongo regardless of cache state.** The previous guard `if (getCachedMonitorChannelId(guildId)) { Mongo update }` skipped the Mongo write when the cache was empty. If `loadMonitorChannelCache` failed at boot (caught and logged), the cache could be empty while Mongo still held a stale `raidChannelId` — `clear` would then no-op persistence and leave the monitor logically still configured. Removed the cache guard; `findOneAndUpdate` without upsert is a cheap no-op when no doc exists anyway. Cache is now treated strictly as a read mirror; writes always go through Mongo first. `src/raid-command.js` (handleRaidChannelCommand clear branch). (Codex round 9, Low.)
- **[LOW] Parser now detects duplicate raid or difficulty aliases as ambiguity errors.** Round 8's parser took the first raid alias and first difficulty alias; subsequent aliases fell through to `leftover` and became part of `charName`. Input like `serca kazeros nightmare clauseduk` produced a misleading "character not found" reply — analogous to the multi-gate ambiguity the parser already handled. Rewrote the token scan to accumulate into `raidSet` / `diffSet` / `gateSet`, then check cardinality: `>1` distinct raid returns `{ error: "multi-raid" }`, `>1` distinct difficulty returns `{ error: "multi-difficulty" }`, same existing multi-gate path. `handleRaidChannelMessage` dispatches all three errors to `postTransientReply` with specific Vietnamese explanations. `src/raid-command.js` (parseRaidMessage rewrite, handleRaidChannelMessage error dispatch). (Codex round 9, Low.)
- **[LOW] `/raid-channel show` falls back to `channels.fetch()` when the channel cache is cold.** The previous implementation read only from `guild.channels.cache.get(channelId)`; right after bot restart the cache may not yet contain the channel even when the bot has access to it, causing `show` to false-positive "channel không truy cập được." The fallback to `guild.channels.fetch(channelId)` is wrapped in try/catch so real errors (deleted channel, denied access) still surface as inaccessible. `src/raid-command.js` (handleRaidChannelCommand show branch). (Codex round 9, Low.)

### Fixed (round 8 Codex review, same-day as feature introduction)

- **[HIGH] `MessageContent` intent is now gated by `TEXT_MONITOR_ENABLED` env var (default `true`).** Originally the bot requested the privileged `MessageContent` intent unconditionally. If the Discord Developer Portal toggle wasn't enabled for the bot application, Discord rejected the login with "Used disallowed intents" and the process exited — taking down slash-command functionality too. `src/bot.js` now reads `process.env.TEXT_MONITOR_ENABLED`; when set to `"false"` the bot skips both the `GuildMessages` + `MessageContent` intents and the `MessageCreate` listener registration, so slash commands (including `/raid-channel` config) remain online even without the portal toggle. Default is `"true"` so existing `.env` files keep the feature on. `src/bot.js`. (Codex round 8, High.)
- **[HIGH] MessageCreate handler no longer hits Mongo on every guild message.** Previously every message the bot could see triggered a `GuildConfig.findOne(...)` lookup before the handler knew whether that channel was even configured — normal chat in active guilds became constant DB reads. Added an in-memory `monitorChannelCache` Map keyed by `guildId`, populated by `loadMonitorChannelCache()` at boot and updated in-place by `/raid-channel set|clear`. Hot path is now a Map lookup; cache miss means instant early-return with no DB work. Also added cheap pre-cache filters for `message.system`, `message.webhookId`, empty content, and non-guild messages. `src/raid-command.js` (new `monitorChannelCache`, `loadMonitorChannelCache`, `getCachedMonitorChannelId`, `setCachedMonitorChannelId`; `handleRaidChannelMessage` + `handleRaidChannelCommand` rewired through it), `src/bot.js` (calls `loadMonitorChannelCache` in `ClientReady`). (Codex round 8, High.)
- **[HIGH] `/raid-channel set` now validates bot channel permissions before persisting.** Previously an admin could register a channel where the bot lacked `View Channel` / `Send Messages` / `Manage Messages` and the command would still reply with a success embed — the monitor would then silently fail on every attempt (can't read, can't reply, can't delete on success). The `set` subcommand now calls `channel.permissionsFor(interaction.guild.members.me)` and replies with the missing permission names if any are absent, leaving the existing config unchanged. `/raid-channel show` grew a parallel health check: if the currently-configured channel is missing permissions or was deleted, `show` surfaces the issue so admins can spot broken config after role changes. `src/raid-command.js` (new `getMissingBotChannelPermissions`, `BOT_CHANNEL_PERMS`; `handleRaidChannelCommand` set/show branches). (Codex round 8, High.)
- **[MEDIUM] Message parser switched from `\b` regexes to explicit tokenization.** The original regex-based parser used `\b` word boundaries, which are ASCII-only in JavaScript — a character name with non-ASCII characters adjacent to an alias substring could false-positive, and the matching-on-whole-string approach also caught aliases embedded inside longer character name tokens (e.g. a name containing "normal" or "kaz"). `parseRaidMessage` now normalizes `act 4` → `act4`, splits on `[\s+,]`, and matches each token against exact alias Maps (`RAID_ALIASES`, `DIFFICULTY_ALIASES`) — boundaries are the whitespace itself, not Unicode-unaware `\b`. Character names like "Normalize", "Hardman", "Kazan", and Korean names remain intact in the `leftover` array. `src/raid-command.js` (new `RAID_ALIASES`, `DIFFICULTY_ALIASES`, `GATE_TOKEN_RE`; rewritten `parseRaidMessage`). (Codex round 8, Medium.)
- **[MEDIUM] Multi-gate messages now reply with a specific error instead of falling through as "character not found".** The old parser only captured the first gate token; extras were swept into the `charName` leftover, producing a misleading "không tìm thấy character" reply when the actual issue was ambiguous intent. The new parser collects every gate via the token scan and, if more than one distinct gate is present with raid+difficulty+char all detected, returns `{ error: "multi-gate", gates }`. The message handler maps that to a transient reply explaining "mỗi lần chỉ update 1 gate". `src/raid-command.js` (parseRaidMessage multi-gate branch, handleRaidChannelMessage error dispatch). (Codex round 8, Medium.)
- **[MEDIUM] `applyRaidSetForDiscordId()` no longer does a duplicate pre-read outside saveWithRetry.** The extraction from `handleRaidSetCommand` preserved an outer `User.findOne` that ran before the retry closure's own `User.findOne`, adding a guaranteed second DB read per write and leaving a stale-pre-check window where the user doc could be created, deleted, or modified between the two reads. The no-roster detection is now inlined into the retry closure and surfaced via a `noRoster` flag that the function returns alongside the other status flags. Single read per attempt, same behavior for both callers (`/raid-set` interaction and the channel text handler). `src/raid-command.js` (applyRaidSetForDiscordId). (Codex round 8, Medium.)

### Added

- **`/raid-channel` command + text-driven raid-clear monitor.** Admin-only slash command (`Manage Server` permission) with three subcommands: `set channel:<channel>` to register a text channel as the monitor target, `show` to display the current config, `clear` to disable. Once registered, every message posted to that channel by non-bot users gets parsed for a pattern `<raid> <difficulty> <character> [gate]` — whitespace or `+` as separators, case-insensitive, any token order. On success the bot applies the same write path as `/raid-set` (single-target character lookup, iLvl eligibility, mode-switch wipe, weekly-reset freshness) and deletes the original message so the channel stays tidy. On known errors (raid not valid, character not in poster's roster, iLvl too low) the bot posts a short reply that auto-deletes after 10s; on parse fail (message wasn't meant to be a raid update) the bot silently ignores. Config lives in a new `guildconfigs` Mongo collection (`src/schema/guildConfig.js`), keyed per-guild. Raid/difficulty aliases: `act 4`/`act4`/`armoche`, `kazeros`/`kaz`, `serca`; `normal`/`nor`, `hard`, `nightmare`/`nm`; gates `G1`..`G9` validated against the raid's real gate list. `src/bot.js` (adds `GuildMessages` + `MessageContent` intents and a `MessageCreate` event handler), `src/raid-command.js` (new `parseRaidMessage`, `handleRaidChannelMessage`, `handleRaidChannelCommand`, `raidChannelCommand` builder, `postTransientReply` helper, HELP_SECTIONS entry), `src/schema/guildConfig.js` (new model). **Deploy prerequisites:** (a) enable the privileged `Message Content Intent` in Discord Developer Portal → Bot → Privileged Gateway Intents, otherwise `message.content` arrives as empty string; (b) grant the bot `View Channel` + `Send Messages` + `Manage Messages` in the target channel for auto-delete to work.
- **Shared `applyRaidSetForDiscordId()` helper extracted from `handleRaidSetCommand`.** The saveWithRetry write path (find user, ensureFreshWeek, resolve single character via findCharacterInUser, iLvl check, mode-switch wipe, gate writes) is now reusable between `/raid-set` and the new text monitor. Both callers get identical write semantics — no duplicated mode-coherence logic, no divergent concurrency handling, no help-docs-drift risk between two parallel implementations. `src/raid-command.js` (new `applyRaidSetForDiscordId`, `handleRaidSetCommand` refactored to delegate).

### Changed

- **Weekly reset moment moved from Wednesday 06:00 UTC to Wednesday 10:00 UTC (= Wednesday 17:00 Vietnam time, UTC+7).** Traine operates the bot on Vietnamese player time and wanted the reset aligned with an early-evening moment for Vietnamese players. The earlier `06:00 UTC` was an initial-scaffold default that translated to 13:00 VN — mid-afternoon, awkward for anyone planning Wednesday-night raid resumption. Shifted the boundary in `getTargetResetKey()` so `passedResetMoment` becomes `utcDay === 0 || utcDay > 3 || (utcDay === 3 && utcHour >= 10)` — Sunday (ISO day 7 post-reset), Thu/Fri/Sat (all after Wed reset), and Wed itself after 10:00 UTC. ISO week boundary (Mon 00:00 UTC) is unchanged; between Mon 00:00 UTC and Wed 10:00 UTC the target key falls back to the previous ISO week via the existing `now - 7 days` branch. Same UTC-only computation, same catch-up semantics, same 30-minute tick cadence — just shifted by 4 hours. README + JSDoc comments on `getTargetResetKey` and `ensureFreshWeek` updated to match. `src/weekly-reset.js` (getTargetResetKey), `README.md`.

### Fixed

- **[MEDIUM] `/raid-status` and `/raid-check` no longer show previous-week completions during the reset catch-up window.** The weekly reset job runs immediately on boot and then every 30 minutes, so a bot that stays online across Wednesday 06:00 UTC can have a short window before the next tick where read-only commands still see the old `weeklyResetKey`. Write paths already call `ensureFreshWeek()`, but `/raid-status` and `/raid-check` did not. `/raid-status` now freshens and saves the user doc inside its existing `saveWithRetry()` render load before lazy roster refresh, and `/raid-check` freshens each lean user snapshot in memory before scanning, so reads align with the current reset target even before the background job persists every user. `src/raid-command.js` (handleStatusCommand, handleRaidCheckCommand). (Codex review round 7, Medium.)
- **[LOW] `refreshStaleAccounts()` skips empty-roster accounts up front.** Direct consequence of round 5's overlap-gate fix: the inner seed-trial loop now requires at least one saved character to overlap with the fetched roster before accepting a seed, but `/remove-roster remove_char` legitimately allows an account to remain at 0 characters. With zero saved characters, overlap can never be true, so the account never stamps `lastRefreshedAt` and every `/raid-status` after cooldown fires another pointless fetch + zero-overlap log line. The `staleAccounts` filter now excludes accounts whose `characters.length === 0` before the cooldown check — nothing useful to fetch there, and `/add-roster` re-stamps on its own save when characters are re-added. `src/raid-command.js` (refreshStaleAccounts). (Codex review round 6, Low.)
- **[MEDIUM] `refreshStaleAccounts()` inner seed-trial loop now validates overlap before accepting a seed.** Round 4's fix only stopped the `lastRefreshedAt` stamp on zero-overlap — the inner loop still returned on the first non-empty fetch, so fallback seeds were never tried and the account kept hitting the same bad first seed every /raid-status call. The inner loop now also checks that at least one saved character appears in the fetched set; on zero overlap it logs and tries the next seed instead of returning early. The account can finally self-heal when the primary seed was stale/wrong but a later fallback seed matches the real roster. The outer `matchedAny` safety check stays as defense-in-depth but is now effectively unreachable. `src/raid-command.js` (refreshStaleAccounts). (Codex review round 5, Medium.)
- **[LOW] `/raid-help` for `/raid-set` now reflects the single-target + status-scoped-gate behavior.** The help example still showed `status:complete gate:G1` (misleading — `gate` is ignored outside `process` since round 3) and the note still claimed duplicate-name characters across two accounts both get updated (false since round 3's single-target fix). Example swapped to `status:process gate:G1`, notes updated to describe `complete`/`reset` as all-gates vs `process` as single-gate, duplicate-name behavior rewritten to "only the first copy by roster order", and a new bullet documents the Serca mode-switch wipe from round 2. `src/raid-command.js` (HELP_SECTIONS raid-set entry). (Codex review round 5, Low.)
- **[MEDIUM] `refreshStaleAccounts()` no longer stamps `lastRefreshedAt` when the fetched roster has zero overlap with saved characters.** The cooldown stamp used to fire unconditionally after any non-empty fetch. But with the seed-fallback path added in round 2, the bot can now end up fetching a different roster (wrong fallback seed hit a foreign account, or upstream top-N churned enough to drop every tracked character). Stamping the cooldown in that case suppressed refresh for 2 hours while the data stayed stale. The loop now tracks `matchedAny` per account — if no saved character matched the fetched set, skip the stamp (so the next `/raid-status` retries) and log a warning with the mismatched roster. `src/raid-command.js` (refreshStaleAccounts). (Codex review round 4, Medium.)
- **[LOW] `accountName` rewrites now guard against collision with another account under the same user.** Both the refresh fallback-seed path and the `/remove-roster remove_char` re-seed path rewrite `accountName` to keep the "name matches a live character" invariant. But `/remove-roster` autocomplete and the removal handler treat `accountName` as a per-user unique id — if two accounts ever converged on the same fallback seed (e.g. the remaining character in account A happens to share a name with account B's seed), the colliding roster would become unaddressable because `findIndex` returns the first match only. Both rewrite sites now check for collision against the user's other accounts and skip the rewrite when one would occur; the account keeps its previous name and the next refresh retries via the other fallback seeds. `src/raid-command.js` (refreshStaleAccounts, handleRemoveRosterCommand). (Codex review round 4, Low.)
- **[LOW] `/raid-status` memoization cache now keys on the character object reference instead of `id || name`.** The previous fallback to normalized character name meant two legacy characters without `character.id` sharing a name across different rosters would share one cache entry — so one card could render the other's progress icons and counts. The render-scoped `Map` now keys on the character reference directly (JS Maps support arbitrary object keys), which is guaranteed unique within a single render pass regardless of id or name state. `src/raid-command.js` (handleStatusCommand raidsCache). (Codex review round 4, Low.)
- **[MEDIUM] `/raid-set` write path now targets exactly one character, matching the autocomplete's view.** `autocompleteRaidSetCharacter` collapses duplicate character names across accounts into a single entry (first-by-iteration), and `findCharacterInUser` returns that same first match — but `handleRaidSetCommand` iterated every account and mutated every copy. If a user genuinely had two characters sharing a name, the dropdown would display eligibility/progress from one copy while the command silently touched all. The handler now resolves the target via `findCharacterInUser(userDoc, characterName)` and writes to that single record only (`updatedCount = 1` at most). `matchedCount` / `ineligibleItemLevel` / `modeResetCount` collapse from accumulators to single-value states. Duplicate character names across rosters remain a not-a-first-class-feature edge case — pinning the write to one target keeps the mental model simple and the UI honest. `src/raid-command.js` (handleRaidSetCommand). (Codex review round 3, Medium.)
- **[LOW] `/raid-set` no longer rejects stale `gate` values that would have been ignored.** The handler used to validate `targetGate` against `getGatesForRaid(raidKey)` before deciding whether gate was relevant at all. If Discord kept a gate carry-over from a prior interaction while the user switched `status` to `complete` or `reset`, the command failed on the stale gate even though `effectiveGate` would have zeroed it out a few lines later. The validation block now lives inside the `statusType === "process"` branch — gate is only validated when it will actually be used. `src/raid-command.js` (handleRaidSetCommand). (Codex review round 3, Low.)
- **[MEDIUM] `normalizeAssignedRaid()` now self-heals legacy mixed-mode raid records on read.** Follow-up from Codex review on the earlier write-path mode-coherence fix: the write path now prevents fresh corruption, but existing docs from before that fix — e.g. a Serca entry with `G1=Nightmare, G2=Hard` both carrying `completedDate` — still rendered wrong progress because `/raid-status` and `/raid-set` autocomplete infer the active mode from `G1.difficulty` first. Normalized docs now pick a single canonical difficulty per raid: the mode that carries the most `completedDate > 0` gates wins (conservation of progress), tie-breaking via `G1.difficulty` → caller's `fallbackDifficulty`. Gates whose stored difficulty no longer matches the canonical mode get their `completedDate` cleared, because Lost Ark weekly entries are mode-scoped — a "minority mode" completion is a spurious claim from the old process-write bug. The function stays pure (callers decide whether to save), so writes through `/raid-set` persist the healed state and the next `/raid-status` for any affected user sees the corrected count. Lazy migration — eager migration script deemed unnecessary for a small bot. `src/raid-command.js` (normalizeAssignedRaid). (Codex review round 2, Medium.)
- **[HIGH] `/raid-set` now re-validates `itemLevel` ≥ raid's `minItemLevel` on the write path.** The autocomplete already hid raids below the character's iLvl threshold, but the handler wrote straight through if the payload was constructed manually (typing a raid value without picking from autocomplete). A user could then pre-mark Serca Hard on a 1720 character and the clear would "appear" retroactively when the character leveled up. Handler now counts matched vs. eligible characters separately: if no matching character meets the iLvl threshold, reply with `⚠️ character đang ở iLvl X, chưa đủ Y+ để thao tác Z`. Matching happens across all accounts by name, so the write proceeds only for the eligible copies. `src/raid-command.js` (handleRaidSetCommand). (Codex review, High.)
- **[HIGH] `/raid-set` now wipes raid progress when the target difficulty differs from any existing gate's difficulty.** Pre-fix, doing `/raid-set raid:serca_hard status:process gate:G2` while `G1` was already stored as `Nightmare` produced a mixed state (`G1=Nightmare, G2=Hard`). The read path derives raid mode from `G1.difficulty` first, so autocomplete and status views for Hard reported `0/2` instead of `1/2` — a display bug that only manifested on Serca's 1740+ triple-mode raid. Write path now compares the new `selectedDifficulty` against every gate's stored difficulty; on mismatch, all gates for that raid are reset (`completedDate` cleared, `difficulty` set to the new one) before the targeted write is applied. The result embed grows a footer explaining the switch when a reset occurred. `src/raid-command.js` (handleRaidSetCommand). (Codex review, High.)
- **[MEDIUM] `/raid-set` ignores `gate` unless `status === "process"`.** The gate autocomplete already returns empty for Complete/Reset, but a stale option value or a pasted payload could still slip through — causing Complete to mark only one gate or Reset to clear only one, contradicting the command's new per-status semantics. Handler now computes `effectiveGate = statusType === "process" ? targetGate : ""` and uses that for the write loop and the result embed's "Gates" field. Complete and Reset always touch every gate now regardless of what arrives in the payload. `src/raid-command.js` (handleRaidSetCommand). (Codex review, Medium.)
- **[MEDIUM] Roster auto-refresh no longer breaks when the seed character is removed or renamed.** Two defenses: (a) `remove-roster action:remove_char` now re-points `account.accountName` to the first remaining character when the removed character was the seed, keeping the "accountName always matches a live character" invariant (footer in the result embed calls it out); and (b) `refreshStaleAccounts` iterates through every character in the account as fallback seeds — if the primary seed fetch fails, it retries with the next name and self-heals `accountName` to whichever seed succeeds. Accounts whose seed got renamed upstream by lostark.bible will now recover automatically on the next `/raid-status`. `src/raid-command.js` (handleRemoveRosterCommand, refreshStaleAccounts). (Codex review, Medium.)

### Added

- **`/raid-set status` gains a new `Process` option for single-gate progression.** Previously `status` only had `Complete` (mark whole raid done) and `Reset` (clear whole raid). Users clearing raids over multiple days had to re-purpose `Complete + gate:G1` to mark individual gates — which was ambiguous UX. `Process` is now the explicit first-class action: it marks exactly one gate as done and **requires** the `gate` option. The handler accepts `process` as an alias of "complete one gate", so the underlying write path is unchanged. Title in the result embed reflects the semantic: `Gate Completed` for process vs `Raid Completed` for complete. `src/raid-command.js` (autocompleteRaidSetStatus, handleRaidSetCommand).

### Changed

- **`/raid-set gate` option flipped from hardcoded `G1|G2|G3` choices to dynamic autocomplete.** The static `addChoices(G1, G2, G3)` was leaking an invalid `G3` for 2-gate raids (Act 4, Kazeros, Serca after the earlier Serca 3→2 gate correction). The new `autocompleteRaidSetGate` reads `getGatesForRaid(raidKey)` from `src/models/Raid.js` and returns exactly the gates the selected raid actually has. It also returns an **empty list unless `status === "process"`**, which functionally "disables" the gate picker for Complete/Reset since those semantically operate on all gates. Combined with the new Process option this produces the flow: pick character → pick raid → pick status=Process → gate dropdown lights up with the raid's real gates. `src/raid-command.js` (new autocompleteRaidSetGate, raidSetCommand builder drops addChoices, handleRaidSetAutocomplete routes `gate` focus, handleRaidSetCommand rejects process-without-gate).
- **`/raid-set` raid-autocomplete now preserves the canonical raid order.** Previously the per-character branch sorted entries by `(rank ASC, minItemLevel DESC, label ASC)` — which produced `Kazeros Hard → Serca Hard → Act 4 Hard → Kazeros Normal → Serca Normal → Act 4 Normal`, mixing raids across difficulty buckets and surprising the user. The new sort respects the source order from `getRaidRequirementList()` (insertion order of `RAID_REQUIREMENTS`: `armoche → kazeros → serca`, and within each raid: `normal → hard → nightmare`). Same order as the `/raid-status` cards so mental model stays consistent. `src/raid-command.js` (autocompleteRaidSetRaid).
- **`/raid-set` highlights fully-completed raids in the dropdown.** Raid options that are 100% done for the selected character now render as `🟢 {label} · {done}/{total} · DONE`, adding a trailing `· DONE` suffix to the existing green-circle icon. Makes it visually unambiguous when the user is about to touch a raid that is already finished. Partials (`🟡`) and pendings (`⚪`) are unchanged. `src/raid-command.js` (autocompleteRaidSetRaid, new shared `computeRaidProgress(character, req)` helper).
- **`/raid-set status` is now autocomplete, and only offers `Reset` when the raid is complete.** The `status` option flipped from `addChoices` (static `Complete` / `Reset`) to `setAutocomplete(true)`. The new `autocompleteRaidSetStatus` handler reads the sibling `character` + `raid` values, reuses `computeRaidProgress`, and returns `[Reset (raid đã hoàn thành — chỉ có thể reset)]` when the raid is already `done === total`. In every other state (no character selected, raid not picked yet, partial, pending) it returns both `Complete` and `Reset` unchanged. Autocomplete is a UX suggestion layer — the handler does not reject spoofed `complete` submissions, but repeat-complete is a safe no-op (it just re-stamps `completedDate` on already-completed gates). `src/raid-command.js` (new autocompleteRaidSetStatus, handleRaidSetAutocomplete routes `status` focus, raidSetCommand builder drops addChoices).

## 2026-04-20

### Added

- Initial bot scaffold: Discord.js v14 client, Mongoose connection, Railway deployment files (`Dockerfile`, `railway.toml`, `.dockerignore`).
- `/add-roster name total?` — sync character roster from `lostark.bible/character/NA/<name>/roster` and persist the top-N characters (default 6, max 6) ranked by combat score.
- `/raid-status` — per-account per-character raid progress view with gate-level granularity (`✅` for full completion, `G1`/`G1/G2` for partial, `❓` for pending).
- `/raid-set character raid status gate?` — mark a raid `complete`/`reset` for a character; optional `gate` (`G1`, `G2`, `G3`) targets a single gate instead of the whole raid.
- `/raid-check raid` — Raid Leader only. Scans every stored character above a raid's minimum item level that has not completed the selected difficulty. Output is auto-paginated into chunks ≤ 1900 chars.
- Three raids with per-mode item level gates: Act 4 (`armoche`) Normal 1700 / Hard 1720, Kazeros Normal 1710 / Hard 1730, Serca Normal 1710 / Hard 1730 / Nightmare 1740.
- Serca dual-mode display: at item level 1740+, `/raid-status` surfaces both Serca Hard and Nightmare as selectable options (Hard alone remains eligible from 1730 via the generic branch).
- Weekly reset job (`src/weekly-reset.js`): 30-minute interval tick that clears gate `completedDate` and task completion counters on Wednesday 06:00 (server local time), idempotent per-user via `weeklyResetKey` (ISO week string).
- 30+ Lost Ark class mappings from `lostark.bible` internal class IDs to display names (`src/models/Class.js`), with title-case fallback for unknown IDs.
- MongoDB DNS fallback in `db.js`: on Atlas SRV `ECONNREFUSED`, automatically retries `mongoose.connect` with configurable DNS servers (default `8.8.8.8,1.1.1.1`).
- `/deploy-commands` hardening: auto-extracts Client ID from a full OAuth2 URL if the user accidentally pastes one into `CLIENT_ID`.
- `/raid-help` — bilingual (EN + VN) help command (originally registered as `/laraidhelp`, renamed before public release). Shows an overview embed listing all four raid commands, plus a dropdown to drill into per-command detail (options, example, notes). Reply is ephemeral so the help doesn't spam the channel.

### Changed

- Raid data model grouped into `armoche` / `kazeros` / `serca`, each exposing a `modes` map with `minItemLevel` per difficulty. Replaces the earlier flat raid list.
- Character data shape migrated to `{id, name, class, itemLevel, combatScore, isGoldEarner, assignedRaids, tasks}`. Per-raid progress now lives inside `assignedRaids.<raidKey>` as a `strict: false` sub-document allowing arbitrary gate keys (`G1`, `G2`, `G3`...).
- Slash command naming and options normalized for consistency (`/add-roster`, `/raid-status`, `/raid-set`, `/raid-check`).
- `/raid-check` completion semantics: now evaluates both difficulty match **and** full-gate completion, not just raid-level boolean.
- `/raid-status` rendering updated with the `✅` / `G1/G2` / `❓` / no-eligible-raids states described above.
- Weekly reset now clears per-gate `completedDate` instead of a raid-level boolean, matching the gate-based progress model.
- UI redesign for `/raid-status` and `/raid-set`:
  - Introduced a module-level `UI` constants block for the bot's color palette (`success` / `progress` / `neutral` / `danger` / `muted`) and status icons (`done` / `partial` / `pending` / `reset` / `lock` / `warn`) so embed styling is centralized.
  - `/raid-status` embed color is now dynamic: green when every eligible raid is done, yellow when anything is in progress, blurple when nothing has started yet.
  - Each raid line in `/raid-status` now renders as `{icon} {raid name} · {done}/{total}` — replacing the previous `{raid name} G1/G2` / `✅` / `❓` formats — so users can see both total gate count and current progress at a glance.
  - `/raid-status` groups characters under `📁 {accountName}` headers and each character line leads with `**name** · class · iLvl` for quick context.
  - `/raid-set` response replaced its plain-text string with a mini embed (Character / Raid / Gates fields) and uses green for `complete` vs muted grey for `reset`.
- Error fallback in `src/bot.js` restored full Vietnamese diacritics ("Có lỗi xảy ra khi xử lý lệnh. Vui lòng thử lại." instead of the accent-stripped form).
- **[Defensive] `deploy-commands.js` now explicitly exits with code 0 after a successful registration.** The discord.js REST client keeps a keep-alive HTTP agent alive internally, so natural event-loop drain never happens after `rest.put(...)` completes. Without an explicit `process.exit(0)` the script hangs forever when run non-interactively (e.g. as a `docker exec` one-off or in a chained startup command). Now exits cleanly. `src/deploy-commands.js`.
- **[BUG FIX] Slash commands now register automatically on bot boot, matching the `LostArk_LoaLogs` pattern.** Earlier this session the Railway `startCommand` was reverted from the chained `node src/deploy-commands.js; node src/bot.js` back to plain `node src/bot.js`. With that revert, `/remove-roster` (and any future command) never reached Discord because nothing on the startup path ran the registration script — the user redeployed Railway and the new command was invisible. `LostArk_LoaLogs` does not have this problem because its `bot.js` calls `rest.put(Routes.applicationCommands/applicationGuildCommands, ...)` inside its `client.once('ready', ...)` handler. Ported the same approach: `src/bot.js` now exposes a `registerSlashCommandsOnBoot(client)` helper that takes `commands[]` from `raid-command.js`, wraps the registration in try/catch (failures log + continue — do not block bot availability), and is awaited from the `ClientReady` handler. Every Railway redeploy now syncs schema for free. `src/deploy-commands.js` is kept for manual one-off invocation from a dev machine. (Codex Railway-sync diagnosis, Root cause CONFIRMED.) `src/bot.js`.
- **[OPT] lostark.bible fetches are now funnelled through a shared concurrency limiter.** Previously `refreshStaleAccounts()` fanned out one `Promise.allSettled` call per stale account with no throttling, and `/add-roster` fired its own independent fetch — so multiple concurrent users all calling `/raid-status` could easily produce a burst of simultaneous requests against lostark.bible. Added a tiny `ConcurrencyLimiter` (max 2 in flight) and routed `fetchRosterCharacters` through `bibleLimiter.run(...)`. A burst now queues politely instead of blasting upstream, sharply reducing the chance of temporary block / 429 throttling. Trade-off: when a user has more than 2 stale accounts, the refreshes serialize slightly instead of running fully in parallel — acceptable cost for predictable upstream behavior. `src/raid-command.js` (new `ConcurrencyLimiter` class, `bibleLimiter` instance, `fetchRosterCharacters` now a thin limiter wrapper around the renamed raw implementation `fetchRosterCharactersRaw`). (Codex optimization review, High.)
- **[OPT] `/raid-status` skips its third Mongo read on the fast path.** Previously the handler did lean pre-check → hydrated load inside `saveWithRetry` → lean re-read before render. The retry body now returns a `doc.toObject()` snapshot and the render uses that directly; the extra `User.findOne()` only runs as an error-path fallback. Saves one DB round-trip per `/raid-status` invocation. `src/raid-command.js` (handleStatusCommand). (Codex optimization review, Medium.)
- **[OPT] `/raid-status` memoizes `getStatusRaidsForCharacter` per render.** Global totals, per-account totals, and per-card rendering previously triggered three separate raid-status computations per character. Added a request-scoped `Map` keyed by character id (or normalized name fallback) and threaded a `getRaidsFor` helper through `buildAccountPageEmbed` and `buildCharacterField`. Render now computes each character's status exactly once. `src/raid-command.js` (handleStatusCommand, buildAccountPageEmbed, buildCharacterField signature change). (Codex optimization review, Medium.)
- **[OPT] Autocomplete paths share an in-flight loader with Mongo dedup.** The four autocomplete handlers (`autocompleteRaidSetCharacter`, `autocompleteRaidSetRaid`, `autocompleteRemoveRosterRoster`, `autocompleteRemoveRosterCharacter`) each used to issue their own `User.findOne().lean()` on every keystroke. They now share `loadUserForAutocomplete(discordId)`, which installs a Map-backed promise cache: a burst of concurrent handlers for the same user collapses to a single DB read, and the entry clears as soon as the promise settles. `src/raid-command.js` (new `loadUserForAutocomplete`, four autocomplete call-site updates). (Codex optimization review, Medium.)
- **`/raid-status` now lazy-refreshes stale accounts against lostark.bible.** New `refreshStaleAccounts(userDoc)` helper is called from `handleStatusCommand` before rendering. For each account whose `lastRefreshedAt` is older than 2 hours (aligned with lostark.bible's upstream update cadence), the bot re-fetches the roster in parallel via `Promise.allSettled`, matches characters by normalized name, and updates only the roster-shape fields (`itemLevel`, `combatScore`, `class`). Raid progress (`assignedRaids`), tasks, and account identity are preserved. Accounts refreshed within the cooldown window hit zero API calls — the bot uses cached data. Individual fetch failures are logged and skipped; the rest of the render continues on whatever data is available. New `accountSchema.lastRefreshedAt` field (default null), stamped by `/add-roster` right before save so the very next `/raid-status` after a fresh add does not re-fetch. `/raid-status` now calls `deferReply()` at the top because the refresh path can take several seconds under a cold cache. `src/schema/user.js`, `src/raid-command.js` (handleStatusCommand, new refreshStaleAccounts, ROSTER_REFRESH_COOLDOWN_MS constant).
- **New `/remove-roster` command.** Lets the user delete either an entire saved roster or a single character inside a specific roster. Three options:
  - `roster` (required, autocomplete) — picks from the user's saved account names; each label shows `📁 {accountName} · N chars`.
  - `action` (required, static choice) — `Remove entire roster` or `Remove a single character`.
  - `character` (conditional, autocomplete chained on `roster`) — required when action is `Remove a single character`; suggestions are drawn from the characters stored in the selected roster and rendered as `{name} · {class} · {iLvl}`.
  Reply is an ephemeral embed confirming the action. Remove-roster splices the account out of the user's `accounts` array; remove-char splices only that character and leaves the account intact (so `/add-roster` can refresh it after removal). Write path is wrapped in `saveWithRetry` for concurrency safety. Pairs naturally with the new strict-duplicate guard in `/add-roster`: users who want to refresh a roster now have a first-party way to delete it first. `src/raid-command.js` (handleRemoveRosterCommand, autocompleteRemoveRosterRoster, autocompleteRemoveRosterCharacter, dispatcher handleRemoveRosterAutocomplete, HELP_SECTIONS entry), `src/bot.js` (allowed-list + autocomplete routing).
- **`/raid-status` pagination buttons reskinned to match the `/list` pattern.** Dropped the disabled middle page-counter button and replaced the emoji-only arrow buttons with text-labelled Secondary-style buttons: `◀ Previous` and `Next ▶`. The page indicator still shows in the embed title (`Page N/M`), so no information is lost; the pagination row just reads cleaner. `src/raid-command.js` (buildStatusPaginationRow).
- **`/add-roster` rejects duplicate roster adds again, by explicit request.** Revert of the earlier "allow refresh" behavior from commit `517cd00` (Codex finding #6). The pre-fetch duplicate guard is back: if the seed name matches either an existing account name or any stored character name under the same Discord user, the command replies ephemerally with `⚠️ Roster đã tồn tại ở account ...` and does not perform the fetch. Traine prefers the stricter "one roster per user unless explicitly removed" semantics over lazy refresh. Users who need to update a saved roster should delete it (future /remove-roster command not yet implemented) and re-add. `src/raid-command.js` (handleAddRosterCommand).
- **[BUG FIX] Legacy Serca G3 gate data no longer inflates the total-gate count.** `normalizeAssignedRaid()` now filters stored gate keys against the raid's current official gate list (`getGatesForRaid(raidKey)`). Data persisted before the Serca-3-to-2-gate metadata correction kept `assignedRaids.serca.G3` around, so `/raid-status` and the `/raid-set` raid autocomplete both displayed Serca entries as `0/3` instead of `0/2`. Normalizing reads filters G3 out of every downstream computation (display counts, `isAssignedRaidCompleted`, progress totals), and callers that reassign `character.assignedRaids = <normalized>` let the DB self-heal on the next write. `src/raid-command.js` (normalizeAssignedRaid).
- **`/raid-status` character card header dropped class name.** Field name shrunk from `{name} · {class} · {iLvl}` to `{name} · {iLvl}`, reducing card width and making the 2-per-row layout less cramped. `/raid-check` was already class-free and stayed unchanged. `src/raid-command.js` (buildCharacterField).
- **`/raid-status` now adds a horizontal gap between paired character cards (width, not height).** Earlier attempt added a row break between pairs, which was vertical spacing — not what was asked for. Reworked: the ZWS spacer is now placed in the MIDDLE inline column so each row reads `[char-left] [gap] [char-right]`, giving the two character cards visible breathing room across the width of the embed instead of stacking them edge-to-edge. The row break between pairs was removed. `src/raid-command.js` (buildAccountPageEmbed).
- **`/raid-status` raid order is now deterministic: Act 4 → Kazeros → Serca.** Replaced the "sort by minItemLevel desc" order with an explicit raid-key precedence table so every character card lists raids top-to-bottom in the same order. Within the same raid (Serca Hard vs Nightmare at 1740+), the *lower* difficulty surfaces first (Serca Hard 1730 above Serca Nightmare 1740) so the tier progression reads Normal → Hard → Nightmare naturally. `src/raid-command.js` (getStatusRaidsForCharacter sort).
- **`/raid-status` rewritten as per-account paginated session, 2 characters per row.**
  - Each saved roster (account) becomes its own page. Navigate with `◀️ / ▶️` buttons; the middle button is a disabled page indicator `(N/M)`.
  - Characters lay out **2 per row** — achieved with an invisible Zero-Width-Space spacer field in the 3rd inline column, forcing the row to wrap after 2 character cards.
  - Page title dynamically reflects that account's aggregate progress (`🟢/🟡/⚪ 📁 {accountName} · Page N/M`), colored green/yellow/blurple accordingly.
  - Description carries per-account progress plus a global summary line across all accounts when more than one page exists.
  - Session timeout = **2 minutes**. Only the invoking user can click the buttons; other users get an ephemeral `🔒 Chỉ người chạy /raid-status...` rejection.
  - On expiry the buttons are disabled and the footer changes to `⏱️ Session đã hết hạn (120s) · Dùng /raid-status để xem lại`.
  - Single-account rosters skip the buttons entirely — direct embed reply, no pagination noise.
  - Multi-embed split code removed since per-page content stays well under Discord limits (max 6 chars × 3 fields = 18 fields per page).
  `src/raid-command.js` (handleStatusCommand, new helpers `buildAccountPageEmbed`, `buildCharacterField`, `buildStatusPaginationRow`).
- **`/raid-status` now lays characters out horizontally, 3 per row on desktop.** Replaced the single-field-per-account stacked list with one *inline* embed field per character. Discord auto-arranges up to three inline fields per row, so a 6-character roster fits in 2 rows instead of scrolling for 6 text blocks. Each character card is `{name} · {class} · {iLvl}` as field name with the raid progress list (`🟢 / 🟡 / ⚪ · done/total`) as the value. The `📁 {accountName}` header stays as a non-inline divider so multi-account rosters keep their grouping. Multi-embed pagination + 1024-char value truncation still applied. `src/raid-command.js` (handleStatusCommand).
- **Corrected Serca gate count from 3 to 2.** `RAID_REQUIREMENTS.serca.gates` now matches the actual in-game layout (`["G1", "G2"]`). Serca-related status counts and `/raid-set` gate validation follow automatically through `getGatesForRaid()`. `src/models/Raid.js`.
- **`/raid-set` `raid` option is now an autocomplete field that filters by the selected character's eligibility AND shows live progress per raid.** When a `character` is already typed or selected:
  - Only raids the character is eligible for (item-level gate passed) are offered.
  - Each suggestion prefixes the raid with a status icon and fraction: `🟢 Act 4 Hard · 2/2`, `🟡 Kazeros Hard · 1/2`, `⚪ Serca Nightmare · 0/2`.
  - Results are ordered pending → partial → done so "work to do" surfaces first.
  - If no character is selected yet, the dropdown falls back to the full raid list with min-item-level tags (`Serca Hard · 1730+`). `src/raid-command.js` (`autocompleteRaidSetRaid`, `findCharacterInUser` helper, dispatcher in `handleRaidSetAutocomplete`).
- **[BUG FIX] `/raid-set` completions no longer vanish when weekly reset fires shortly afterward.** Previously, `/raid-set` (and `/add-roster` when refreshing) wrote `assignedRaids.<raid>.<gate>.completedDate` but did not update `user.weeklyResetKey`, so the very next weekly-reset tick — which runs every 30 minutes and resets any user whose key lags `getTargetResetKey()` — would clear the just-set completion because the user still looked stale. A new `ensureFreshWeek(user)` helper (exported from `src/weekly-reset.js`) now runs at the start of every write path's `saveWithRetry` closure: if the user's `weeklyResetKey` lags the current target, it clears all characters' gate completions and task counters and bumps the key before the write proceeds, so the write is always on a fresh-week baseline and the reset job skips the user on its next tick. Idempotent no-op when already fresh. `src/weekly-reset.js` (new `ensureFreshWeek`), `src/raid-command.js` (handleAddRosterCommand + handleRaidSetCommand retry bodies).
- Changed the "done" status icon from `✅` (green check) to `🟢` (green circle) so it sits visually beside `🟡 partial` and `⚪ pending` as a matched circle set. Because `UI.icons.done` is the single source, this propagates to `/raid-status` (title icon, footer legend, per-raid lines), `/raid-set` success embed, `/raid-check` empty-state embed, and `/raid-help` detail notes in one change.
- `/raid-set` `character` option now autocompletes from the caller's saved roster. Handler `handleRaidSetAutocomplete` is wired into `InteractionCreate` (via `interaction.isAutocomplete()`) and surfaces up to 25 results as `{name} · {class} · {iLvl}`, deduped across accounts, sorted by item level desc. Needle match is substring (case-insensitive), so users can type any part of the character name. `src/raid-command.js`, `src/bot.js`.
- Fixed the discord.js v14 deprecation warning "Supplying `ephemeral` for interaction response options is deprecated. Utilize flags instead." Every `{ ephemeral: true }` across the bot was replaced with `{ flags: MessageFlags.Ephemeral }` (13 call sites total, plus the shared error fallback in `src/bot.js`). Functional behavior is unchanged — only the flag encoding differs. `src/raid-command.js`, `src/bot.js`.
- Silenced the `Could not parse CSS stylesheet` noise that jsdom emits when loading the lostark.bible roster page. Introduced a module-level `VirtualConsole` that swallows that specific `jsdomError` while still surfacing anything else; passed to `new JSDOM(html, { virtualConsole })` in `fetchRosterCharacters`. Scraping output is unchanged; the log is now clean. `src/raid-command.js`.
- Follow-up fixes from the Codex re-review of `517cd00`:
  - **[MEDIUM] `/add-roster` refresh now primarily matches the seed name before falling back to character overlap.** A re-registered roster whose stored top-N no longer overlaps the freshly fetched top-N (churn, gear reorganization, name changes) would have been pushed as a second duplicate account under the post-fix logic. The matcher now checks `normalizeName(accountName) === seed` first, then any stored character name equalling the seed, then the original overlap heuristic. `src/raid-command.js` (handleAddRosterCommand retry body).
  - **[MEDIUM] `/raid-set` now validates `gate` against the raid's actual gate list.** The slash command still exposes G1/G2/G3 for every raid, but the handler now rejects (with an ephemeral `⚠️` message listing valid gates) before mutating when the submitted `targetGate` is not in `getGatesForRaid(raidKey)`. Prevents synthesizing a phantom `G3` on two-gate raids. `src/raid-command.js` (handleRaidSetCommand validation block).
  - **[LOW] Weekly reset `modifiedCount` no longer over-reports.** The retry callback now returns a boolean indicating whether a save actually happened; `modifiedCount` increments only on `true`, so a stale user already refreshed by another worker between the initial scan and the per-user retry no longer inflates the log line. `src/weekly-reset.js` (resetWeekly loop).
- Slash command raid-choice labels changed from `Act 4 Normal (>= 1700)` to `Act 4 Normal · 1700+`. The `·` middle dot separator and `+` suffix are shorter, easier to scan in the Discord dropdown, and drop the awkward `(>=` glyph. Touches `getRaidRequirementChoices()` in `src/models/Raid.js`.
- UI polish sweep across all commands (consistency pass):
  - `/add-roster` success embed: title now `📥 Roster Synced` with a source footer, color switched from magic hex `0x57f287` to `UI.colors.success`, character-list header renamed to `Characters (N)` for symmetry with other commands.
  - Added `UI.icons.info` (`ℹ️`) and `UI.icons.roster` (`📥`) to the centralized icon set.
  - All plain-text error / info interaction responses now lead with a status icon (`⚠️` for validation errors, `ℹ️` for "no roster yet", `🔒` for role gates) and carry full Vietnamese diacritics. Covers: `/add-roster` fetch failure + empty-roster, `/raid-check` + `/raid-set` invalid raid option, `/raid-set` invalid status, `/raid-set` character-not-found, `/raid-status` + `/raid-set` no-roster-yet.
- `/raid-check` upgraded from plain-text chunks to a proper embed:
  - Dynamic color by difficulty: red for Nightmare, yellow for Hard, blurple for Normal.
  - Results grouped by Discord user (most pending characters first, characters sorted by item level within each user).
  - Multi-embed pagination when results exceed 25 fields or 5500 chars total — first via `editReply`, subsequent via ephemeral `followUp`.
  - Empty state (everyone has completed the raid) now renders a green success embed instead of a plain sentence.
  - Role-gate rejection (`Chỉ Raid Leader mới được dùng /raid-check`) restored full Vietnamese diacritics.
- `getStatusRaidsForCharacter` now also exposes `allGateKeys` (derived from the character's stored `assignedRaids` sub-document) so the UI layer can render an accurate `done/total` ratio instead of guessing gate totals.

### Fixed

- Option-name mismatches between slash command definitions and handler code.
- Command routing and interaction handling for renamed commands.
- `/add-roster` combat-score parsing and sorting when values contain `~`, `,`, or other non-numeric symbols (now stripped before `parseFloat`).
- `/raid-check` item-level filter: characters with missing or non-numeric `itemLevel` were slipping past the minimum-item-level gate because `Number(undefined) < n` evaluates to `false` (`NaN` comparisons are always `false`). The filter now coerces via `Number() || 0` before comparison. (commit `dfb19e9`)
- Added a clarifying comment on the `1740+` Serca branch in `/raid-status` documenting why item level 1740+ surfaces Hard **and** Nightmare as dual options (Hard alone remains eligible from 1730 via the generic branch). (commit `dfb19e9`)

### Bug fixes (Codex review)

- **[HIGH] Fixed lost-update races in `/add-roster`:** both first-time setup (concurrent requests observing an empty user) and subsequent refreshes could clobber each other or trip `E11000` on the `discordId` unique index. Switched to `saveWithRetry()` which re-fetches + reapplies the mutation on `VersionError` or duplicate-key, and enabled Mongoose optimistic concurrency on the `User` schema (`optimisticConcurrency: true` replaced the previous `versionKey: false`). `src/raid-command.js` (handleAddRosterCommand), `src/schema/user.js`.
- **[HIGH] Fixed lost-update races in `/raid-set`:** two `/raid-set` commands on different characters of the same user could overwrite each other since the handler saved the entire `User` document. Now wrapped in `saveWithRetry()` so the mutation is reapplied on a fresh document if another command commits in between. `src/raid-command.js` (handleRaidSetCommand).
- **[HIGH] Weekly reset no longer overwrites live command writes.** The reset job now fetches a minimal list of stale user IDs, then for each user re-fetches + mutates + saves inside `saveWithRetry()`. If a user's document is modified by `/raid-set` between the fetch and the save, the reset retries on the fresh doc instead of silently rewriting it. `src/weekly-reset.js`.
- **[HIGH] Weekly reset now catches up if the process was offline across the Wednesday 06:00 UTC window.** Replaced the hard "skip unless Wednesday morning" guard with a `getTargetResetKey()` comparison — any user whose `weeklyResetKey` lags the current target (last reset moment that has passed, in UTC) gets reset on the next tick regardless of what day the bot wakes up. `src/weekly-reset.js`.
- **[HIGH] Weekly reset timing is now entirely UTC-based.** The prior code mixed local-time trigger (`getDay()` / `getHours()`) with UTC week-key computation, so the same bot on two hosts would reset at two different real-world instants. `isWednesdayMorning()` was replaced by `getTargetResetKey()` which uses `getUTCDay()` / `getUTCHours()` consistently with `getWeekKey()`. `src/weekly-reset.js`.
- **[MEDIUM] `/add-roster` now refreshes an existing roster instead of rejecting it.** Removed the pre-fetch duplicate guard that rejected any seed name matching an existing account or character — the later merge path already handled the refresh correctly. `src/raid-command.js` (handleAddRosterCommand).
- **[MEDIUM] Per-raid gate metadata.** `RAID_REQUIREMENTS` now exposes `gates` per raid (Act 4 / Kazeros = `G1, G2`, Serca = `G1, G2, G3`) via a `getGatesForRaid(raidKey)` helper. Every hardcoded `["G1", "G2"]` fallback was replaced so Serca now tracks three gates end-to-end (legacy migration, `normalizeAssignedRaid`, `formatRaidStatusLine`, and `/raid-set` default-init). `src/models/Raid.js`, `src/raid-command.js`.
- **[MEDIUM] `/raid-status` no longer fails when the total embed would exceed Discord's 6000-character limit.** The handler now builds one base embed and pushes additional `(continued)` embeds whenever the next account field would push total size past 5500 chars or field count past 25. The first embed is sent via `reply`; the rest as `followUp`. `src/raid-command.js` (handleStatusCommand).
- **[LOW] Roster class-name scraper tolerates optional whitespace and escaped characters.** The `name:"...",class:"..."` regex is now `/name:\s*"((?:[^"\\]|\\.)*)"\s*,\s*class:\s*"((?:[^"\\]|\\.)*)"/g` with a small `unescapeJsonLike` post-processor, so `name:\"Foo\\\"Bar\",class:\"bard\"` and minor upstream formatting changes no longer silently degrade class names to `Unknown`. `src/raid-command.js` (extractRosterClassMapFromHtml).

### Deployment

- Slash commands registered per-guild via `applicationGuildCommands` (not global) after each schema change.
- `src/deploy-commands.js` validates that `CLIENT_ID` and `GUILD_ID` are 17-20 digit Discord snowflakes before issuing the REST put, and reports a clear error on `404` pointing at likely misconfigurations (wrong client id, bot not in guild).

### Known Limitations

- Server local time drives weekly reset timing. Railway containers default to UTC → Wed 06:00 UTC.
- `/raid-check` role gate is exact-match on role name `raid leader` (case-insensitive).
- Two legacy files from the bot template are not imported by the current bot and can be removed: `config.js` and `src/models/GuildConfig.js` (both reference LoaLogs-specific features absent here — officer approvers, Gemini, ScraperAPI, `/lasetup`, `/laremote`, blacklist scope).
